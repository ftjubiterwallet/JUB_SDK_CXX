编译工程文件已经迁移到CMake
1.安装CMake 3+
2.Mac和Linux需要安装hidapi
2.mkdir build & cd build & cmake ..
3.make

libJUB_SDK.a for iOS compiling:
1. mkdir build
2. cd build
3. > cmake .. -G Xcode -DCMAKE_TOOLCHAIN_FILE=../cmake/ios.toolchain.cmake -DPLATFORM=OS64COMBINED
4. > cmake --build . --config Release --target
or simply proceed
> sh ./tools/ios-build